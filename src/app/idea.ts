import { detail } from './detail';
import { Url } from 'url';

// tslint:disable-next-line: class-name
export class idea {
    title: string;
    detailList: detail[];
    imgUrl: string;
    date: Date;
}