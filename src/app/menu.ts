import { Url } from 'url';
import { SafeHtml } from '@angular/platform-browser';

export class menu{
    number:number;
    menuName:string;
    url_sanitaize:SafeHtml;
    url_string:string;
    route:string;
}